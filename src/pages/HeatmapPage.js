import React from "react";
import { Box, Typography } from "@mui/material";

export default function HeatmapPage() {
  return (
    <Box p={3}>
      <Typography variant="h4">Correlation Heatmap</Typography>
      {/* Add heatmap chart here */}
    </Box>
  );
}
